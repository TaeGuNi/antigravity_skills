# 🚀 Antigravity Agent Core Skills (The 22 Protocols)

> **엔터프라이즈급 AI 코딩 에이전트를 위한 "무자비한(Ruthless) 아키텍처 법전"**

이 레포지토리는 단순한 프롬프트 모음집이 아닙니다. LLM 에이전트(AI)가 로컬 터미널과 인프라에 직접 접근하여 코드를 작성하고 시스템을 제어할 때, **개발자의 의도를 벗어난 "할루시네이션 코딩"과 "스파게티 아키텍처"를 원천 차단하기 위해 뇌(Brain) 구조에 영구적으로 주입되는 22개의 헌법(Constitution)**입니다.

과거의 느리고 뚱뚱한 레거시 도구들을 타파하고, **오직 I/O 속도의 극한과 절대 죽지 않는 무결성(Stability)**만을 추구하는 시니어 엔지니어링 철학을 강제합니다.

> 🚨 **AI 에이전트 절대 경고 (No Lazy Reading)**:
> 여기에 적힌 문장은 1줄 요약본에 불과합니다. 이 파일만 읽고 섣불리 코드를 작성하거나 설계하지 마십시오. 현재 작업 중인 도메인(프론트, 백, 디버깅 등)에 해당하는 **실제 `SKILL.md` 원본 파일 경로를 `view_file` 도구로 찾아 최우선적으로 정독(Read)한 뒤 설계에 착수해야 합니다.**

---

## 🔥 4대 핵심 아키텍처 철학 (Core Philosophy)

1. **I/O 병목 파괴 (I/O Maximization)**
   느려 터진 `npm`, `yarn(v1)`, `pip` 사용을 "반역"으로 규정하고, 수십 배 빠른 **`pnpm`**, **`uv`** 같은 초고속 네이티브 런타임만을 강제합니다.
2. **무자비한 타입 가디언 (Ruthless Type Safety)**
   "당장 돌아가는 코드"는 AI의 달콤한 거짓말입니다. 로직 전반에 **Zod 런타임 스키마 검증**을 태워 "죽지 않는 코드"를 강제시킵니다.
3. **상태의 파괴 (State Annihilation)**
   프론트엔드와 백엔드를 막론하고 인메모리 '상태(State)'가 가져오는 부작용과 확장성 저하를 원천 차단하는 Stateless(무상태) 및 URL Query 패러다임을 지향합니다.
4. **완전 자율형 GitOps 및 치유 (Autonomous E2E)**
   코드만 짜주고 멈추는 에이전트의 시대는 끝났습니다. 터미널 에러 시 핑계 없이 자가 치유(Self-Healing)를 수행하고, GitHub CLI(`gh`)를 통해 PR 생성부터 머지(Merge)까지 기계적으로 완수합니다.

---

## 🏗️ 탑재된 22대 코어 스킬 토폴로지 (Skill Layers)

에이전트는 단순히 22개의 스킬을 던져 받는 것이 아니라, 하단의 **5계층(Layer) 아키텍처 분할**에 따라 자신의 역할을 인지해야 합니다.

### 🧠 Layer 0: AI 메타 인지 및 파일시스템 통제 (Meta-Cognition & Nomenclature)
이 스킬들은 에이전트의 "자아"와 "파일 단위의 물리적 격리"를 통제하며, 모든 레이어 작업 시 항상 배경(Background) 컨텍스트로 깔려 있어야 합니다.

- [**`absolute_naming_protocol/`**](./absolute_naming_protocol/SKILL.md): **[핵심 마스터 헌법]** 모노레포와 TS/JS 생태계에 적용되는 파일시스템 절대 네이밍 규칙. 쓰레기통 네이밍(`utils`) 금지, Zod DTO 강제, AST 리팩토링 철학.
- [**`zero_trust_coding/`**](./zero_trust_coding/SKILL.md): 할루시네이션 및 Deprecated 된 레거시 문법을 원천 차단하기 위한 에이전트 자가 검열 헌법.
- [**`ruthless_reviewer/`**](./ruthless_reviewer/SKILL.md): 칭찬을 배제하고 시간/공간 복잡도와 동시성 폭발, 엣지 케이스를 스파르타식으로 물고 늘어지는 가학적 코드 리뷰어 자아.
- [**`epistemic_humility/`**](./epistemic_humility/SKILL.md): 자신이 짠 코드의 확신도(Confidence)를 %로 선언하고, 50% 미만 시 섣부른 파일 쓰기를 멈추고 인간에게 결정권(Context)을 넘기는 인식론적 겸손.
- [**`self_healing/`**](./self_healing/SKILL.md): 터미널 에러 시 핑계 없이 스스로 Stack Trace를 읽고 최대 3회 루프를 돌며 자가 치유(Zombie Mode)하는 터미널 장악 스킬.
- [**`markdown_source_of_truth/`**](./markdown_source_of_truth/SKILL.md): 대화창이 길어져 발생하는 AI 치매(Context 망각)를 방어하기 위해 설계/히스토리를 마크다운 로컬 파일로 압축-영구 보존.

### ⚡ Layer 1: 아키텍처 런타임 (Foundation & Runtime)
언어 프레임워크와 의존성이 설치되는 인프라의 I/O 생태계를 강제합니다.

- [**`typescript_core/`**](./typescript_core/SKILL.md): `npm`/`eslint` 금지. 패키징은 `pnpm`, 타입 검증은 `Zod`, 린팅은 `Biome`, 테스트는 `Vitest`로 통일하는 JS/TS 생태계 I/O 최적화 룰.
- [**`uv_python/`**](./uv_python/SKILL.md): `pip`/`venv` 구조를 폐기하고, Rust 기반의 `uv`만 사용하여 스크립트 실행(`uv run`)과 의존성 관리(`uv add`)를 완전히 격리하는 파이썬 룰.

### 🗄️ Layer 2: 서버 및 데이터베이스 아키텍처 (Backend & Data Tier)
백엔드 로직 작성 및 데이터 설계 시 절대 어겨서는 안 되는 무결성 및 확장성의 뼈대입니다.

- [**`ddd_architecture/`**](./ddd_architecture/SKILL.md): 거대 함수를 찢고, View-Business-Data 3계층의 완벽한 관심사 분리(SoC)를 강제하는 도메인 주도 설계(DDD) 지침.
- [**`stateless_architecture/`**](./stateless_architecture/SKILL.md): 백엔드 메모리 의존성을 박탈시키고, JWT 인가 및 캐시용 외부 Redis 위임을 통해 클라우드 스케일아웃 시 서버 폭발을 지연시키는 무상태 헌법.
- [**`rdbms_architecture/`**](./rdbms_architecture/SKILL.md): 특정 벤더에 종속되지 않는 3대 핵심 스키마 추상화 설계 원칙 (가장 작은 타입 강제, 범용 ANSI 조인 지향, No-JSON).
- **RDBMS 파생본 (Readonly 안전 래퍼 스크립트 팩)**:
  - [**`postgresql/`**](./postgresql/SKILL.md) / [**`mysql/`**](./mysql/SKILL.md) / [**`mariadb/`**](./mariadb/SKILL.md)

### 🎨 Layer 3: 프론트엔드 및 렌더링 (Frontend & UI Tier)
SPA/SSR 프론트엔드 코드 작성 시 발생하는 컴포넌트 파편화와 픽셀/컬러 오염을 린터 레벨에서 폭파합니다.

- [**`react_next_architecture/`**](./react_next_architecture/SKILL.md): 서버 컴포넌트(RSC) 환경에서 `use client` 말단 격리, URL Query를 통한 상태 치환, Prop Drilling 3단계 근절(Zustand) 법칙.
- [**`tailwind_architecture/`**](./tailwind_architecture/SKILL.md): 매직 넘버(임의의 색상/픽셀, `[]`) 주입을 원천 차단하고 단일 진실 공급원(`cva`)으로 UI를 강제 추상화하는 Tailwind 디자인 통제.
- [**`design_system_identity/`**](./design_system_identity/SKILL.md): 거대 사이트에서 일관성을 유지하는 3계층 Semantic 토큰, 8px 공간 리듬 배수, 그리고 완벽한 WAI-ARIA 인지적 웹 접근성(A11y/Contrast)을 지배하는 시각 헌법.

### 🛡️ Layer 4: 보안, 관측성 및 인프라 (DevOps & Defense Tier)
프로덕션 배포 전 서버의 생존력(Survival)을 담보하는 최종 방벽입니다.

- [**`security_iam_protocol/`**](./security_iam_protocol/SKILL.md): NextAuth/JWT 체제에서 토큰 탈취(XSS)를 원천 차단하는 `HttpOnly` 쿠키 강제, Refresh 활주로 분리, 로그인 Rate-Limiting(Brute Force 방어) 보안 헌법.
- [**`observability_logging_protocol/`**](./observability_logging_protocol/SKILL.md): `console.log` 쓰레기를 쓸어버리고 Pino/Winston 기반의 구조적 JSON 로깅(`{ action: "..." }`) 및 Sentry 에러 바운더리 리포트 자동화를 통제하는 관측 감시 헌법.
- [**`github_cli/`**](./github_cli/SKILL.md): GitHub CLI(`gh`)를 활용한 4단계 완전 자율 GitOps 파이프라인. 병합 충돌 시 자동 Rebase.
- [**`docker/`**](./docker/SKILL.md): 컨테이너 접근 시 에이전트 행오버(-it) 방어, 폐쇄망(`$DOCKER_REGISTRY_URL`) 격리, 쓰레기 없는 `--rm` 디버깅 선언 룰.
- [**`chaos_monkey_testing/`**](./chaos_monkey_testing/SKILL.md): 정상 흐름(Happy Path) 단위 테스트를 경멸하고, 자발적 데이터 파괴, 네트워크 타임아웃, OOM 등 극한 재난 복구(Disaster Recovery) 시나리오만 린팅하는 테스트 철학.
- [**`playwright_ref_protocol/`**](./playwright_ref_protocol/SKILL.md): 전체 DOM 전달에 따른 막대한 토큰 낭비를 절감(90%)하고, Shadow DOM 투과 및 시맨틱 힌트를 결합하여 오류 없는 초고속 E2E 테스트 자동화를 강제하는 브라우저 탐색 헌법.

---

## 💊 상황별 스킬 주입 레시피 (Agent Starter Packs)

에이전트가 어떤 과업을 수행하느냐에 따라 아래의 "스타터 팩(Skill Combinations)" 경로를 우선적으로 정독(RAG 주입)하도록 명령하십시오. 

> **Tip**: 모든 명령의 기저에는 반드시 **Layer 0 (자아/인지 통제 체계 및 절대 헌법)** 스킬들이 무의식 중에 깔려 있어야 에이전트가 오작동하지 않습니다.

| 과업 (Task) | 필수 주입 레이어 및 핵심 스킬 목록 (Read these `.md` files first!) |
| :--- | :--- |
| **프론트엔드 UI/UX 설계** | `Layer 3` (`react_next`, `tailwind`, `design_system`) + `Layer 1` (`typescript_core`) |
| **API / 백엔드 비즈니스 로직** | `Layer 2` (`ddd`, `stateless`, `rdbms`) + `Layer 1` (`typescript_core` or `uv_python`) |
| **인증 시스템 / 로그인 튜닝** | `Layer 4` (`security_iam_protocol`) + `Layer 2` (`stateless_architecture`) |
| **디버깅 / 에러 트래킹 추적** | `Layer 0` (`self_healing`, `absolute_naming_protocol`) + `Layer 4` (`observability_logging_protocol`) |
| **DevOps 인프라 셋업 / 배포** | `Layer 4` (`docker`, `github_cli`) + `Layer 0` (`markdown_source_of_truth`) |
| **코드 리팩토링 / 무결성 검증** | `Layer 0` (`ruthless_reviewer`, `absolute_naming_protocol`) + `Layer 4` (`chaos_monkey_testing`) |
